# pnpm 配置文件
# 使用 pnpm 的严格模式，确保依赖版本一致性
strict-peer-dependencies=false

# 自动安装 peer dependencies
auto-install-peers=true

# 允许运行安装后脚本（Electron 需要）
enable-pre-post-scripts=true

# 允许 ffmpeg-static 运行构建脚本（需要下载 FFmpeg 可执行文件）
# 注意：这会在安装时下载较大的 FFmpeg 二进制文件
ignore-scripts=false

# 注意：ignore-scripts=false 在 .npmrc 中不起作用
# 需要在安装时使用 --ignore-scripts=false 参数
# 或者手动运行: node node_modules\.pnpm\electron@33.4.11\node_modules\electron\install.js

# Electron 需要提升到根目录（解决 Windows 上的路径问题）
# 这是 Electron 在 pnpm 下的已知问题，需要将 electron 提升到 node_modules 根目录
public-hoist-pattern[]=*electron*
public-hoist-pattern[]=*electron-builder*

# 提升其他可能需要全局访问的工具
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*

# 使用符号链接（pnpm 的默认行为，节省磁盘空间）
# 如果需要，可以设置为 false 使用硬链接
# symlink=true

# 存储路径（可选，默认在用户目录）
# store-dir=~/.pnpm-store

# 并发安装数量
# fetch-concurrency=16

# 网络配置
# registry=https://registry.npmjs.org/

# 禁用自动更新检查（可选）
# update-notifier=false

